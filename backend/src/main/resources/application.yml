logging:
  level:
    sql: debug

spring:
  datasource:
    url: ***REMOVED***
    username: marketplace_admin
    password: ***REMOVED***

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
